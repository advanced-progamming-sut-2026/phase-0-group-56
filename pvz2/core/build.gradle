[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'
eclipse.project.name = appName + '-core'

dependencies {
  api "com.badlogicgames.gdx:gdx:$gdxVersion"
  api "com.badlogicgames.gdx:gdx-freetype:$gdxVersion"

  // Use the checked-in library source so gameplay renderers can use the
  // visibility-aware scaled PAM overload.
  implementation project(':libPVZ-0.1.5')
  implementation 'com.github.pizpizi:pvz-skin:v0.1.1'
  implementation 'com.github.raeleus.TenPatch:tenpatch:5.2.3'
  if(enableGraalNative == 'true') {
    implementation "io.github.berstanio:gdx-svmhelper-annotations:$graalHelperVersion"
  }
}

jar {
  duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}
